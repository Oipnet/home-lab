# Copier en .env et remplir les valeurs.
# Ce fichier est gitignore — ne jamais committer .env

# ── Scaleway ──────────────────────────────────────────────────────────────────
SCW_ACCESS_KEY="SCWXXXXXXXXXXXXXXXXX"
SCW_SECRET_KEY="xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
SCW_DEFAULT_PROJECT_ID="xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

# ── Plex — stockage ───────────────────────────────────────────────────────────
NAS_WG_IP="10.200.0.10"
NAS_NFS_PATH="/volume1/k8s"

# ── WireGuard — génération config client ──────────────────────────────────────
# Remplir après le déploiement (affiché par le playbook Ansible wireguard.yml)
SERVER_PUBLIC_IP=""
SERVER_WG0_PUBKEY=""
