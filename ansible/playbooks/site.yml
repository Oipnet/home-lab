---
# Master playbook — run in order
- import_playbook: common.yml
- import_playbook: kubernetes.yml
- import_playbook: haproxy.yml
- import_playbook: wireguard.yml
# Tunnel NAS : exécuter séparément (voir wireguard-nas.yml et scripts/gen-wireguard-nas.sh)
# - import_playbook: wireguard-nas.yml
